# Refonte UX type Odoo — Tranche 1 (Socle + Verticale Ventes) — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Donner à DaCompta un shell de navigation type Odoo, un framework de primitives UI réutilisables, et la verticale Ventes de bout en bout (facture client → paiement → lettrage → balance âgée).

**Architecture:** Next.js App Router. Server Components chargent via server actions (cookie `dossierId`) ; Client Components pour l'interactif, mutation puis `router.refresh()`. Primitives UI explicites composées à la main (pas de moteur de métadonnées). CSS maison, aucune lib UI externe.

**Tech Stack:** Next.js 16, React 19, Prisma 6 (SQLite), TypeScript, Vitest (+ jsdom + @testing-library/react pour les primitives).

## Global Constraints

- Code commenté en français ; commits conventionnels (`feat:`, `fix:`, `chore:`, `test:`, `docs:`).
- TDD obligatoire : test d'abord, vu échouer, puis implémentation minimale.
- `npx vitest run`, `npx tsc --noEmit`, `npm run build` doivent rester verts avant chaque commit de fin de tâche.
- Aucune dépendance UI externe (CSS maison). Seules deps de test autorisées : `jsdom`, `@testing-library/react`, `@testing-library/dom`.
- Toutes les server actions prennent `dossierId` ; ne jamais appeler Prisma depuis un composant client — passer par `src/server/*`.
- Devise : tout montant via `arrondiDevise` / `estNulDevise` (jamais d'arrondi en dur).
- BDD de test : `test.db`, reset depuis les migrations par `src/server/test-setup.ts`. Toute migration qui reconstruit une table à CHECK doit rétablir les CHECK (cf. `20260620110000_recheck_constraints`).

---

## Phase A — Backend (préalable à l'UI)

### Task A1 : Champ `Journal.type` (façon `journal.type` Odoo)

**Files:**
- Modify: `prisma/schema.prisma` (modèle `Journal`)
- Create: `prisma/migrations/<timestamp>_journal_type/migration.sql` (généré)
- Modify: `prisma/seed.ts` (types des journaux)
- Modify: `src/server/test-helpers.ts` (journaux de test typés si créés)
- Test: `src/server/journal.test.ts`

**Interfaces:**
- Produces: `Journal.type: string` ∈ `{sale, purchase, cash, bank, misc}` (défaut `misc`).

- [ ] **Step 1 : Écrire le test qui échoue**

```ts
// src/server/journal.test.ts
import { describe, it, expect, beforeEach } from "vitest";
import { prisma } from "@/lib/db";
import { resetDb } from "./test-helpers";

let dossierId: string;
beforeEach(async () => { dossierId = await resetDb(); });

describe("Journal.type", () => {
  it("stocke le type d'un journal (sale)", async () => {
    const j = await prisma.journal.create({
      data: { code: "VT", libelle: "Ventes", type: "sale", dossierId },
    });
    expect(j.type).toBe("sale");
  });
  it("défaut = misc", async () => {
    const j = await prisma.journal.create({
      data: { code: "OD", libelle: "Opérations diverses", dossierId },
    });
    expect(j.type).toBe("misc");
  });
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npx vitest run src/server/journal.test.ts`
Expected: FAIL — `type` inconnu sur `JournalCreateInput` (erreur de type / colonne absente).

- [ ] **Step 3 : Ajouter le champ au schéma**

Dans `prisma/schema.prisma`, modèle `Journal`, après `libelle` :
```prisma
  type      String  @default("misc") // sale | purchase | cash | bank | misc
```

- [ ] **Step 4 : Générer la migration**

Run: `$env:PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION="yes POC"; npx prisma migrate dev --name journal_type --skip-seed`
Expected: migration créée, client régénéré. (Journal n'a pas de contrainte CHECK → rien à rétablir.)

- [ ] **Step 5 : Mettre à jour le seed**

Dans `prisma/seed.ts`, remplacer le tableau `JOURNAUX` par des entrées typées :
```ts
  const JOURNAUX = [
    { code: "ACH", libelle: "Achats", type: "purchase" },
    { code: "VT", libelle: "Ventes", type: "sale" },
    { code: "CAI", libelle: "Caisse", type: "cash" },
    { code: "BIMA", libelle: "Banque", type: "bank" },
    { code: "OD", libelle: "Opérations diverses", type: "misc" },
    { code: "PE", libelle: "Paie", type: "misc" },
    { code: "RAN", libelle: "Report à nouveau", type: "misc" },
  ];
  for (const j of JOURNAUX) {
    await prisma.journal.upsert({
      where: { dossierId_code: { dossierId: dossier.id, code: j.code } },
      update: { libelle: j.libelle, type: j.type },
      create: { code: j.code, libelle: j.libelle, type: j.type, dossierId: dossier.id },
    });
  }
```

- [ ] **Step 6 : Vérifier le succès + suite complète**

Run: `npx vitest run src/server/journal.test.ts` → PASS
Run: `npx vitest run` → tout vert
Run: `npx tsc --noEmit` → OK

- [ ] **Step 7 : Commit**

```bash
git add prisma/schema.prisma prisma/migrations prisma/seed.ts src/server/journal.test.ts
git commit -m "feat(compta): type de journal (sale/purchase/cash/bank/misc)"
```

---

### Task A2 : `listerFactures(dossierId, filtre)`

**Files:**
- Create: `src/server/factures.ts`
- Test: `src/server/factures.test.ts`

**Interfaces:**
- Consumes: `creerFacture`, `validerPiece`, `creerTaxe`, `creerTiers`, `creerCompte` (existants) ; `Journal.type` (A1).
- Produces:
```ts
export interface FactureListItem {
  id: string; numeroPiece: string; datePiece: string; statut: string;
  tiersNom: string | null; montantTTC: number;
  etatPaiement: "NON_PAYE" | "PARTIEL" | "PAYE";
}
export interface FiltreFactures { statut?: "BROUILLON" | "VALIDEE" | "ANNULEE"; texte?: string; }
export function listerFactures(dossierId: string, filtre?: FiltreFactures): Promise<FactureListItem[]>;
```

- [ ] **Step 1 : Écrire le test qui échoue**

```ts
// src/server/factures.test.ts
import { describe, it, expect, beforeEach } from "vitest";
import { prisma } from "@/lib/db";
import { resetDb, seedComptesStandards } from "./test-helpers";
import { creerCompte } from "./comptes";
import { creerTiers } from "./tiers";
import { creerTaxe, creerFacture } from "./taxes";
import { validerPiece } from "./pieces";
import { listerFactures } from "./factures";

let dossierId: string; let journalVente: string;
beforeEach(async () => {
  dossierId = await resetDb();
  await seedComptesStandards(dossierId);
  await creerCompte({ dossierId, numeroSaisi: "411100", intitule: "Clients", collectif: true });
  await creerTaxe({ dossierId, code: "TVA18", nom: "TVA 18%", taux: 18, usage: "sale", compteNumero: "443100" });
  journalVente = (await prisma.journal.create({ data: { code: "VT", libelle: "Ventes", type: "sale", dossierId } })).id;
});

it("liste les factures du journal de vente avec tiers, TTC et état de paiement", async () => {
  const client = await creerTiers({ dossierId, code: "C001", nom: "Alpha", type: "CLIENT" });
  const f = await creerFacture({
    dossierId, journalId: journalVente, numeroPiece: "VT-1", sens: "VENTE",
    tiersId: client.id, compteTiersNumero: "411100",
    lignes: [{ compteNumero: "707000", libelleLigne: "Vente", montantHT: 100_000, taxeCode: "TVA18" }],
  });
  await validerPiece(f.id);

  const liste = await listerFactures(dossierId);
  expect(liste).toHaveLength(1);
  expect(liste[0].tiersNom).toBe("Alpha");
  expect(liste[0].montantTTC).toBe(118_000);
  expect(liste[0].etatPaiement).toBe("NON_PAYE");
});

it("filtre par statut et par texte (numéro/tiers)", async () => {
  const client = await creerTiers({ dossierId, code: "C001", nom: "Alpha", type: "CLIENT" });
  const f = await creerFacture({
    dossierId, journalId: journalVente, numeroPiece: "VT-2", sens: "VENTE",
    tiersId: client.id, compteTiersNumero: "411100",
    lignes: [{ compteNumero: "707000", libelleLigne: "Vente", montantHT: 50_000, taxeCode: "TVA18" }],
  });
  await validerPiece(f.id);
  expect(await listerFactures(dossierId, { statut: "VALIDEE" })).toHaveLength(1);
  expect(await listerFactures(dossierId, { statut: "BROUILLON" })).toHaveLength(0);
  expect(await listerFactures(dossierId, { texte: "alpha" })).toHaveLength(1);
  expect(await listerFactures(dossierId, { texte: "zzz" })).toHaveLength(0);
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npx vitest run src/server/factures.test.ts`
Expected: FAIL — module `./factures` introuvable.

- [ ] **Step 3 : Implémenter `listerFactures`**

```ts
// src/server/factures.ts
"use server";

import { prisma } from "@/lib/db";
import { getEtatPaiementFacture, type EtatPaiementFacture } from "./paiements";

export interface FactureListItem {
  id: string;
  numeroPiece: string;
  datePiece: string;
  statut: string;
  tiersNom: string | null;
  montantTTC: number;
  etatPaiement: EtatPaiementFacture;
}

export interface FiltreFactures {
  statut?: "BROUILLON" | "VALIDEE" | "ANNULEE";
  texte?: string;
}

/** Factures clients = pièces des journaux de vente (journal.type = "sale"). */
export async function listerFactures(
  dossierId: string,
  filtre: FiltreFactures = {},
): Promise<FactureListItem[]> {
  const pieces = await prisma.piece.findMany({
    where: {
      dossierId,
      journal: { type: "sale" },
      ...(filtre.statut ? { statut: filtre.statut } : {}),
    },
    include: { lignes: { include: { tiers: { select: { nom: true } } } } },
    orderBy: { numeroPiece: "asc" },
  });

  const items = await Promise.all(
    pieces.map(async (p) => {
      const ligneTiers = p.lignes.find((l) => l.tiersId != null);
      return {
        id: p.id,
        numeroPiece: p.numeroPiece,
        datePiece: p.datePiece.toISOString(),
        statut: p.statut,
        tiersNom: ligneTiers?.tiers?.nom ?? null,
        montantTTC: Number(p.montantTTC),
        etatPaiement: await getEtatPaiementFacture(dossierId, p.id),
      };
    }),
  );

  if (!filtre.texte) return items;
  const t = filtre.texte.toLowerCase();
  return items.filter(
    (i) => i.numeroPiece.toLowerCase().includes(t) || (i.tiersNom ?? "").toLowerCase().includes(t),
  );
}
```

- [ ] **Step 4 : Vérifier le succès**

Run: `npx vitest run src/server/factures.test.ts` → PASS

- [ ] **Step 5 : Commit**

```bash
git add src/server/factures.ts src/server/factures.test.ts
git commit -m "feat(ventes): listerFactures (tiers, TTC, état paiement)"
```

---

### Task A3 : `getFacture(dossierId, id)` (document + compteurs smart buttons)

**Files:**
- Modify: `src/server/factures.ts`
- Test: `src/server/factures.test.ts` (ajout)

**Interfaces:**
- Consumes: `getEtatPaiementFacture`.
- Produces:
```ts
export interface FactureLigne {
  id: string; compteNumero: string; libelleLigne: string;
  debit: number; credit: number; tiersNom: string | null; taxeCode: string | null;
}
export interface FactureDetail {
  id: string; numeroPiece: string; datePiece: string; statut: string;
  journalCode: string; tiersId: string | null; tiersNom: string | null;
  montantHT: number; montantTVA: number; montantTTC: number;
  etatPaiement: "NON_PAYE" | "PARTIEL" | "PAYE";
  nbPaiements: number; estLettree: boolean;
  lignes: FactureLigne[];
}
export function getFacture(dossierId: string, id: string): Promise<FactureDetail>;
```

- [ ] **Step 1 : Écrire le test qui échoue (ajout au fichier)**

```ts
// src/server/factures.test.ts (append)
import { getFacture } from "./factures";
import { enregistrerPaiement } from "./paiements";

it("getFacture renvoie en-tête, lignes et compteurs smart buttons", async () => {
  const client = await creerTiers({ dossierId, code: "C001", nom: "Alpha", type: "CLIENT" });
  await prisma.journal.create({ data: { code: "CAI", libelle: "Caisse", type: "cash", dossierId } });
  const f = await creerFacture({
    dossierId, journalId: journalVente, numeroPiece: "VT-9", sens: "VENTE",
    tiersId: client.id, compteTiersNumero: "411100",
    lignes: [{ compteNumero: "707000", libelleLigne: "Vente", montantHT: 100_000, taxeCode: "TVA18" }],
  });
  await validerPiece(f.id);

  const detail = await getFacture(dossierId, f.id);
  expect(detail.montantTTC).toBe(118_000);
  expect(detail.tiersNom).toBe("Alpha");
  expect(detail.lignes.length).toBeGreaterThanOrEqual(3); // 707, 443, 411
  expect(detail.nbPaiements).toBe(0);
  expect(detail.estLettree).toBe(false);

  const journalCaisse = (await prisma.journal.findFirstOrThrow({ where: { dossierId, code: "CAI" } })).id;
  await enregistrerPaiement({
    dossierId, journalId: journalCaisse, numeroPiece: "CAI-9", sens: "ENTRANT",
    tiersId: client.id, compteTresorerieNumero: "521000", compteTiersNumero: "411100", montant: 118_000,
  });
  const apres = await getFacture(dossierId, f.id);
  expect(apres.etatPaiement).toBe("PAYE");
  expect(apres.estLettree).toBe(true);
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npx vitest run src/server/factures.test.ts -t getFacture`
Expected: FAIL — `getFacture` non exporté.

- [ ] **Step 3 : Implémenter `getFacture`**

```ts
// src/server/factures.ts (append)
export interface FactureLigne {
  id: string; compteNumero: string; libelleLigne: string;
  debit: number; credit: number; tiersNom: string | null; taxeCode: string | null;
}
export interface FactureDetail {
  id: string; numeroPiece: string; datePiece: string; statut: string;
  journalCode: string; tiersId: string | null; tiersNom: string | null;
  montantHT: number; montantTVA: number; montantTTC: number;
  etatPaiement: EtatPaiementFacture; nbPaiements: number; estLettree: boolean;
  lignes: FactureLigne[];
}

export async function getFacture(dossierId: string, id: string): Promise<FactureDetail> {
  const p = await prisma.piece.findFirstOrThrow({
    where: { id, dossierId },
    include: {
      journal: { select: { code: true } },
      lignes: {
        orderBy: { ordre: "asc" },
        include: { tiers: { select: { id: true, nom: true } }, taxe: { select: { code: true } } },
      },
    },
  });
  const ligneTiers = p.lignes.find((l) => l.tiersId != null);
  // Nb de lettrages touchant la ligne de tiers de la facture = "paiements" rapprochés.
  const nbPaiements = ligneTiers
    ? await prisma.lettrage.count({
        where: { OR: [{ ligneDebitId: ligneTiers.id }, { ligneCreditId: ligneTiers.id }] },
      })
    : 0;
  const estLettree = ligneTiers ? Number(ligneTiers.amountResidual) === 0 : false;
  return {
    id: p.id, numeroPiece: p.numeroPiece, datePiece: p.datePiece.toISOString(), statut: p.statut,
    journalCode: p.journal.code, tiersId: ligneTiers?.tiersId ?? null, tiersNom: ligneTiers?.tiers?.nom ?? null,
    montantHT: Number(p.montantHT), montantTVA: Number(p.montantTVA), montantTTC: Number(p.montantTTC),
    etatPaiement: await getEtatPaiementFacture(dossierId, p.id),
    nbPaiements, estLettree,
    lignes: p.lignes.map((l) => ({
      id: l.id, compteNumero: l.compteNumero, libelleLigne: l.libelleLigne,
      debit: Number(l.debit), credit: Number(l.credit),
      tiersNom: l.tiers?.nom ?? null, taxeCode: l.taxe?.code ?? null,
    })),
  };
}
```

- [ ] **Step 4 : Vérifier le succès + suite**

Run: `npx vitest run src/server/factures.test.ts` → PASS
Run: `npx vitest run && npx tsc --noEmit` → vert

- [ ] **Step 5 : Commit**

```bash
git add src/server/factures.ts src/server/factures.test.ts
git commit -m "feat(ventes): getFacture (document + compteurs smart buttons)"
```

---

## Phase B — UI

### Task B0 : Environnement de test composant (jsdom + Testing Library)

**Files:**
- Modify: `package.json` (devDependencies + script inchangé)
- Modify: `vitest.config.ts` (environnement par fichier)
- Create: `src/components/test-setup-dom.ts`

**Interfaces:**
- Produces: capacité de tester des composants React en Vitest (`environment: jsdom` via commentaire de fichier `// @vitest-environment jsdom`).

- [ ] **Step 1 : Installer les dépendances de test**

Run: `npm i -D jsdom @testing-library/react @testing-library/dom`
Expected: ajout aux devDependencies, pas d'erreur.

- [ ] **Step 2 : Setup DOM (cleanup auto)**

```ts
// src/components/test-setup-dom.ts
import { afterEach } from "vitest";
import { cleanup } from "@testing-library/react";
afterEach(() => cleanup());
```

- [ ] **Step 3 : Autoriser jsdom par fichier dans vitest**

Dans `vitest.config.ts`, `test.environmentMatchGlobs` est déprécié ; on utilise le commentaire `// @vitest-environment jsdom` en tête des fichiers de test composant. Vérifier que `environment: "node"` reste le défaut (inchangé). Aucune modification de config nécessaire si le commentaire par fichier est utilisé. Ajouter `setupFiles` conditionnel :
```ts
// vitest.config.ts — dans test: { ... }
    setupFiles: ["./src/components/test-setup-dom.ts"],
```
(Le cleanup est inerte côté node.)

- [ ] **Step 4 : Test de fumée**

```tsx
// src/components/Badge.test.tsx
// @vitest-environment jsdom
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";

function Hello() { return <span>ok</span>; }

describe("env jsdom", () => {
  it("rend un composant", () => {
    render(<Hello />);
    expect(screen.getByText("ok")).toBeTruthy();
  });
});
```

Run: `npx vitest run src/components/Badge.test.tsx`
Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
git add package.json package-lock.json vitest.config.ts src/components/test-setup-dom.ts src/components/Badge.test.tsx
git commit -m "test(ui): environnement de test composant (jsdom + testing-library)"
```

---

### Task B1 : Mappers purs (badge état, aperçu TTC)

**Files:**
- Create: `src/lib/ui/facture-ui.ts`
- Test: `src/lib/ui/facture-ui.test.ts`

**Interfaces:**
- Consumes: `computeTaxe` (`src/lib/comptabilite/taxe.ts`).
- Produces:
```ts
export function badgeEtatPaiement(e: "NON_PAYE"|"PARTIEL"|"PAYE"): { label: string; variant: "warn"|"ok"|"muted" };
export interface LigneSaisieHT { montantHT: number; taux: number; }
export function apercuTotaux(lignes: LigneSaisieHT[], devise: string): { ht: number; tva: number; ttc: number };
```

- [ ] **Step 1 : Écrire le test qui échoue**

```ts
// src/lib/ui/facture-ui.test.ts
import { describe, it, expect } from "vitest";
import { badgeEtatPaiement, apercuTotaux } from "./facture-ui";

describe("badgeEtatPaiement", () => {
  it("mappe l'état vers libellé + variante", () => {
    expect(badgeEtatPaiement("PAYE").variant).toBe("ok");
    expect(badgeEtatPaiement("PARTIEL").variant).toBe("warn");
    expect(badgeEtatPaiement("NON_PAYE").variant).toBe("muted");
  });
});

describe("apercuTotaux", () => {
  it("agrège HT/TVA/TTC en XOF (0 décimale)", () => {
    const r = apercuTotaux([{ montantHT: 100_000, taux: 18 }, { montantHT: 50_000, taux: 18 }], "XOF");
    expect(r.ht).toBe(150_000);
    expect(r.tva).toBe(27_000);
    expect(r.ttc).toBe(177_000);
  });
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npx vitest run src/lib/ui/facture-ui.test.ts`
Expected: FAIL — module introuvable.

- [ ] **Step 3 : Implémenter**

```ts
// src/lib/ui/facture-ui.ts
import { Prisma } from "@prisma/client";
import { computeTaxe } from "@/lib/comptabilite/taxe";
import { arrondiDevise } from "@/lib/comptabilite/devise";

export function badgeEtatPaiement(e: "NON_PAYE" | "PARTIEL" | "PAYE"): { label: string; variant: "warn" | "ok" | "muted" } {
  switch (e) {
    case "PAYE": return { label: "Payé", variant: "ok" };
    case "PARTIEL": return { label: "Partiel", variant: "warn" };
    default: return { label: "Non payé", variant: "muted" };
  }
}

export interface LigneSaisieHT { montantHT: number; taux: number; }

/** Aperçu HT/TVA/TTC d'une saisie de facture, arrondi à la devise. */
export function apercuTotaux(lignes: LigneSaisieHT[], devise: string): { ht: number; tva: number; ttc: number } {
  let ht = new Prisma.Decimal(0), tva = new Prisma.Decimal(0);
  for (const l of lignes) {
    const r = computeTaxe(l.montantHT, l.taux, { priceInclude: false, typeAmount: "percent", devise });
    ht = ht.plus(r.baseHT);
    tva = tva.plus(r.montantTaxe);
  }
  const ttc = arrondiDevise(ht.plus(tva), devise);
  return { ht: ht.toNumber(), tva: tva.toNumber(), ttc: ttc.toNumber() };
}
```

- [ ] **Step 4 : Vérifier le succès**

Run: `npx vitest run src/lib/ui/facture-ui.test.ts` → PASS

- [ ] **Step 5 : Commit**

```bash
git add src/lib/ui/facture-ui.ts src/lib/ui/facture-ui.test.ts
git commit -m "feat(ui): mappers purs facture (badge état, aperçu TTC)"
```

---

### Task B2 : Cookie dossier + primitive `<Shell>`

**Files:**
- Create: `src/lib/dossier-context.ts` (lecture/écriture cookie côté serveur)
- Create: `src/server/dossiers.ts` (lister/sélectionner dossiers)
- Create: `src/components/Shell.tsx` (Server Component)
- Create: `src/components/DossierSwitcher.tsx` (Client)
- Modify: `src/app/layout.tsx` (retirer l'ancienne nav, garder `<html>/<body>` + import CSS)
- Test: `src/server/dossiers.test.ts`

**Interfaces:**
- Produces:
```ts
// src/lib/dossier-context.ts
export function getDossierIdCookie(): Promise<string | null>;
export function setDossierIdCookie(id: string): Promise<void>;
// src/server/dossiers.ts
export function listerDossiers(): Promise<{ id: string; nom: string }[]>;
export function getDossierCourant(): Promise<{ id: string; nom: string } | null>;
```

- [ ] **Step 1 : Écrire le test qui échoue (server dossiers)**

```ts
// src/server/dossiers.test.ts
import { describe, it, expect, beforeEach } from "vitest";
import { resetDb } from "./test-helpers";
import { listerDossiers } from "./dossiers";

beforeEach(async () => { await resetDb(); });

it("liste les dossiers (au moins celui de test)", async () => {
  const ds = await listerDossiers();
  expect(ds.length).toBeGreaterThanOrEqual(1);
  expect(ds[0]).toHaveProperty("nom");
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npx vitest run src/server/dossiers.test.ts`
Expected: FAIL — module introuvable.

- [ ] **Step 3 : Implémenter cookie + dossiers**

```ts
// src/lib/dossier-context.ts
import { cookies } from "next/headers";
const COOKIE = "dossierId";
export async function getDossierIdCookie(): Promise<string | null> {
  const c = await cookies();
  return c.get(COOKIE)?.value ?? null;
}
export async function setDossierIdCookie(id: string): Promise<void> {
  const c = await cookies();
  c.set(COOKIE, id, { path: "/", sameSite: "lax" });
}
```

```ts
// src/server/dossiers.ts
"use server";
import { prisma } from "@/lib/db";
import { getDossierIdCookie, setDossierIdCookie } from "@/lib/dossier-context";

export async function listerDossiers() {
  return prisma.dossier.findMany({ select: { id: true, nom: true }, orderBy: { nom: "asc" } });
}
export async function getDossierCourant() {
  const id = await getDossierIdCookie();
  if (!id) return null;
  return prisma.dossier.findUnique({ where: { id }, select: { id: true, nom: true } });
}
export async function choisirDossier(id: string) {
  await setDossierIdCookie(id);
}
```

- [ ] **Step 4 : Vérifier le succès (server)**

Run: `npx vitest run src/server/dossiers.test.ts` → PASS

- [ ] **Step 5 : Implémenter `<DossierSwitcher>` (client)**

```tsx
// src/components/DossierSwitcher.tsx
"use client";
import { useRouter } from "next/navigation";
import { choisirDossier } from "@/server/dossiers";

export function DossierSwitcher({ dossiers, courantId }: { dossiers: { id: string; nom: string }[]; courantId: string | null }) {
  const router = useRouter();
  return (
    <select
      className="input"
      value={courantId ?? ""}
      onChange={async (e) => { await choisirDossier(e.target.value); router.refresh(); }}
    >
      {!courantId && <option value="">— choisir un dossier —</option>}
      {dossiers.map((d) => <option key={d.id} value={d.id}>{d.nom}</option>)}
    </select>
  );
}
```

- [ ] **Step 6 : Implémenter `<Shell>` (server) + config de modules**

```tsx
// src/components/Shell.tsx
import type { ReactNode } from "react";
import { listerDossiers, getDossierCourant } from "@/server/dossiers";
import { DossierSwitcher } from "./DossierSwitcher";

// Modules du switcher (T1 : Ventes, Tiers, États actifs).
const MODULES = [
  { key: "ventes", label: "Ventes", href: "/ventes/factures", actif: true },
  { key: "tiers", label: "Tiers", href: "/tiers", actif: true },
  { key: "etats", label: "États", href: "/etats", actif: true },
  { key: "achats", label: "Achats", href: "#", actif: false },
  { key: "banque", label: "Banque", href: "#", actif: false },
  { key: "compta", label: "Compta", href: "#", actif: false },
];

// Sous-menu par module.
const SOUS_MENUS: Record<string, { label: string; href: string }[]> = {
  ventes: [
    { label: "Factures clients", href: "/ventes/factures" },
    { label: "Paiements", href: "/ventes/paiements" },
    { label: "Balance âgée", href: "/ventes/balance-agee" },
  ],
  tiers: [{ label: "Tiers", href: "/tiers" }],
  etats: [{ label: "États & documents", href: "/etats" }],
};

export async function Shell({ module, breadcrumb, action, children }: {
  module: keyof typeof SOUS_MENUS | string;
  breadcrumb: { label: string; href?: string }[];
  action?: ReactNode;
  children: ReactNode;
}) {
  const [dossiers, courant] = await Promise.all([listerDossiers(), getDossierCourant()]);
  const sousMenu = SOUS_MENUS[module] ?? [];
  return (
    <div>
      <header style={{ display: "flex", alignItems: "center", gap: 16, padding: "10px 24px", borderBottom: "2px solid var(--line)", background: "var(--panel)" }}>
        <a href="/" style={{ fontWeight: 700, fontFamily: "'Space Mono', monospace", color: "inherit", textDecoration: "none" }}>DaCompta</a>
        <nav style={{ display: "flex", gap: 12 }}>
          {MODULES.map((m) => (
            <a key={m.key} href={m.actif ? m.href : "#"}
               style={{ color: m.actif ? "inherit" : "var(--muted)", textDecoration: "none", fontWeight: m.key === module ? 700 : 400 }}
               aria-disabled={!m.actif} title={m.actif ? "" : "À venir"}>{m.label}</a>
          ))}
        </nav>
        <div style={{ marginLeft: "auto", display: "flex", alignItems: "center", gap: 12 }}>
          <DossierSwitcher dossiers={dossiers} courantId={courant?.id ?? null} />
          <span className="chip" title="Authentification différée">👤 Comptable</span>
        </div>
      </header>
      {sousMenu.length > 0 && (
        <nav style={{ display: "flex", gap: 16, padding: "8px 24px", borderBottom: "1px solid var(--line)", background: "var(--bg)" }}>
          {sousMenu.map((s) => <a key={s.href} href={s.href} style={{ color: "inherit", textDecoration: "none" }}>{s.label}</a>)}
        </nav>
      )}
      <div style={{ padding: "8px 24px", display: "flex", alignItems: "center", gap: 8 }}>
        <div className="muted" style={{ flex: 1 }}>
          {breadcrumb.map((b, i) => (
            <span key={i}>{i > 0 && " › "}{b.href ? <a href={b.href} style={{ color: "inherit" }}>{b.label}</a> : b.label}</span>
          ))}
        </div>
        {action}
      </div>
      <main className="container">{!courant ? <EcranAucunDossier /> : children}</main>
    </div>
  );
}

function EcranAucunDossier() {
  return <div className="panel" style={{ padding: 24 }}><p>Aucun dossier sélectionné. Choisissez-en un dans la barre du haut.</p></div>;
}
```

- [ ] **Step 7 : Brancher le layout**

Remplacer le contenu de `src/app/layout.tsx` par :
```tsx
import "./globals.css";
import type { ReactNode } from "react";
export const metadata = { title: "DaCompta" };
export default function RootLayout({ children }: { children: ReactNode }) {
  return (<html lang="fr"><body>{children}</body></html>);
}
```
(Le `<Shell>` est invoqué par chaque page de module, pas par le layout — il dépend du module/breadcrumb courant.)

- [ ] **Step 8 : Vérifier build + suite**

Run: `npx tsc --noEmit && npx vitest run` → vert
Run: `npm run build` → compile (les pages existantes restent valides ; le shell n'est pas encore utilisé par une page neuve).

- [ ] **Step 9 : Commit**

```bash
git add src/lib/dossier-context.ts src/server/dossiers.ts src/server/dossiers.test.ts src/components/Shell.tsx src/components/DossierSwitcher.tsx src/app/layout.tsx
git commit -m "feat(ui): shell Odoo (barre module + dossier cookie + breadcrumb)"
```

---

### Task B3 : Primitive `<DataTable>`

**Files:**
- Create: `src/components/DataTable.tsx` (Client)
- Test: `src/components/DataTable.test.tsx`

**Interfaces:**
- Produces:
```ts
export interface Colonne<T> { cle: string; titre: string; rendu: (row: T) => ReactNode; }
export function DataTable<T>(props: {
  colonnes: Colonne<T>[]; lignes: T[]; cleLigne: (row: T) => string;
  onLigneClick?: (row: T) => void; rechercheTexte?: boolean; videLabel?: string;
}): JSX.Element;
```

- [ ] **Step 1 : Écrire le test qui échoue**

```tsx
// src/components/DataTable.test.tsx
// @vitest-environment jsdom
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import { DataTable } from "./DataTable";

describe("DataTable", () => {
  it("affiche les lignes et l'état vide", () => {
    render(<DataTable colonnes={[{ cle: "n", titre: "Nom", rendu: (r: { nom: string }) => r.nom }]}
                      lignes={[{ nom: "Alpha" }]} cleLigne={(r) => r.nom} />);
    expect(screen.getByText("Alpha")).toBeTruthy();
  });
  it("affiche le libellé vide quand aucune ligne", () => {
    render(<DataTable colonnes={[{ cle: "n", titre: "Nom", rendu: (r: { nom: string }) => r.nom }]}
                      lignes={[]} cleLigne={(r) => r.nom} videLabel="Rien" />);
    expect(screen.getByText("Rien")).toBeTruthy();
  });
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npx vitest run src/components/DataTable.test.tsx`
Expected: FAIL — module introuvable.

- [ ] **Step 3 : Implémenter**

```tsx
// src/components/DataTable.tsx
"use client";
import { useState, type ReactNode } from "react";

export interface Colonne<T> { cle: string; titre: string; rendu: (row: T) => ReactNode; }

export function DataTable<T>({ colonnes, lignes, cleLigne, onLigneClick, rechercheTexte, videLabel = "Aucun élément." }: {
  colonnes: Colonne<T>[]; lignes: T[]; cleLigne: (row: T) => string;
  onLigneClick?: (row: T) => void; rechercheTexte?: boolean; videLabel?: string;
}) {
  const [q, setQ] = useState("");
  const filtrees = rechercheTexte && q
    ? lignes.filter((r) => colonnes.some((c) => String(c.rendu(r) ?? "").toLowerCase?.().includes?.(q.toLowerCase())))
    : lignes;
  return (
    <div>
      {rechercheTexte && (
        <input className="input" placeholder="Rechercher…" value={q} onChange={(e) => setQ(e.target.value)} style={{ marginBottom: 12 }} />
      )}
      <div style={{ overflowX: "auto" }}>
        <table style={{ width: "100%", borderCollapse: "collapse" }}>
          <thead>
            <tr>{colonnes.map((c) => <th key={c.cle} style={{ textAlign: "left", borderBottom: "1px solid var(--line)", padding: 8 }}>{c.titre}</th>)}</tr>
          </thead>
          <tbody>
            {filtrees.length === 0 ? (
              <tr><td colSpan={colonnes.length} className="muted" style={{ padding: 16 }}>{videLabel}</td></tr>
            ) : filtrees.map((r) => (
              <tr key={cleLigne(r)} onClick={() => onLigneClick?.(r)} style={{ cursor: onLigneClick ? "pointer" : "default" }}>
                {colonnes.map((c) => <td key={c.cle} style={{ borderBottom: "1px solid var(--line)", padding: 8 }}>{c.rendu(r)}</td>)}
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
}
```

- [ ] **Step 4 : Vérifier le succès**

Run: `npx vitest run src/components/DataTable.test.tsx` → PASS

- [ ] **Step 5 : Commit**

```bash
git add src/components/DataTable.tsx src/components/DataTable.test.tsx
git commit -m "feat(ui): primitive DataTable (recherche, clic ligne, état vide)"
```

---

### Task B4 : Primitives `<StatusBar>` + `<SmartButton>` + `<Badge>`

**Files:**
- Create: `src/components/StatusBar.tsx`
- Create: `src/components/SmartButton.tsx`
- Create: `src/components/Badge.tsx`
- Test: `src/components/StatusBar.test.tsx`

**Interfaces:**
- Produces:
```ts
export function StatusBar(props: { etats: string[]; courant: string; actions?: ReactNode }): JSX.Element;
export function SmartButton(props: { icone: string; compteur?: number; label: string; href?: string }): JSX.Element;
export function Badge(props: { label: string; variant?: "ok"|"warn"|"muted" }): JSX.Element;
```

- [ ] **Step 1 : Écrire le test qui échoue**

```tsx
// src/components/StatusBar.test.tsx
// @vitest-environment jsdom
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import { StatusBar } from "./StatusBar";

describe("StatusBar", () => {
  it("met en évidence l'état courant", () => {
    render(<StatusBar etats={["Brouillon", "Validé"]} courant="Validé" />);
    const courant = screen.getByText("Validé");
    expect(courant.getAttribute("data-courant")).toBe("true");
  });
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npx vitest run src/components/StatusBar.test.tsx`
Expected: FAIL — module introuvable.

- [ ] **Step 3 : Implémenter les trois primitives**

```tsx
// src/components/Badge.tsx
export function Badge({ label, variant = "muted" }: { label: string; variant?: "ok" | "warn" | "muted" }) {
  const bg = variant === "ok" ? "#dcfce7" : variant === "warn" ? "#fef9c3" : "#f1f5f9";
  const fg = variant === "ok" ? "#166534" : variant === "warn" ? "#854d0e" : "#475569";
  return <span className="badge" style={{ background: bg, color: fg, padding: "2px 8px", borderRadius: 6 }}>{label}</span>;
}
```

```tsx
// src/components/SmartButton.tsx
export function SmartButton({ icone, compteur, label, href }: { icone: string; compteur?: number; label: string; href?: string }) {
  const inner = (
    <span style={{ display: "inline-flex", alignItems: "center", gap: 6, border: "1px solid var(--line)", borderRadius: 8, padding: "6px 10px" }}>
      <span>{icone}</span>{compteur != null && <strong>{compteur}</strong>}<span className="muted">{label}</span>
    </span>
  );
  return href ? <a href={href} style={{ textDecoration: "none", color: "inherit" }}>{inner}</a> : inner;
}
```

```tsx
// src/components/StatusBar.tsx
import type { ReactNode } from "react";
export function StatusBar({ etats, courant, actions }: { etats: string[]; courant: string; actions?: ReactNode }) {
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 12, padding: "8px 0", borderBottom: "1px solid var(--line)" }}>
      <div style={{ display: "flex", gap: 8 }}>{actions}</div>
      <div style={{ marginLeft: "auto", display: "flex", gap: 4 }}>
        {etats.map((e) => (
          <span key={e} data-courant={e === courant}
            style={{ padding: "4px 10px", borderRadius: 6, background: e === courant ? "var(--accent)" : "transparent", color: e === courant ? "#fff" : "var(--muted)" }}>{e}</span>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 4 : Vérifier le succès**

Run: `npx vitest run src/components/StatusBar.test.tsx` → PASS

- [ ] **Step 5 : Commit**

```bash
git add src/components/StatusBar.tsx src/components/SmartButton.tsx src/components/Badge.tsx src/components/StatusBar.test.tsx
git commit -m "feat(ui): primitives StatusBar, SmartButton, Badge"
```

---

### Task B5 : Page liste des factures (`/ventes/factures`)

**Files:**
- Create: `src/app/ventes/factures/page.tsx` (Server Component)
- Create: `src/app/ventes/factures/FacturesClient.tsx` (Client : DataTable + navigation)

**Interfaces:**
- Consumes: `listerFactures` (A2), `getDossierIdCookie`, `<Shell>`, `<DataTable>`, `<Badge>`, `badgeEtatPaiement`.

- [ ] **Step 1 : Écrire le client liste**

```tsx
// src/app/ventes/factures/FacturesClient.tsx
"use client";
import { useRouter } from "next/navigation";
import { DataTable } from "@/components/DataTable";
import { Badge } from "@/components/Badge";
import { badgeEtatPaiement } from "@/lib/ui/facture-ui";
import type { FactureListItem } from "@/server/factures";

export function FacturesClient({ factures }: { factures: FactureListItem[] }) {
  const router = useRouter();
  return (
    <DataTable
      rechercheTexte
      lignes={factures}
      cleLigne={(f) => f.id}
      onLigneClick={(f) => router.push(`/ventes/factures/${f.id}`)}
      videLabel="Aucune facture. Créez-en une avec « + Nouvelle facture »."
      colonnes={[
        { cle: "num", titre: "Numéro", rendu: (f) => f.numeroPiece },
        { cle: "date", titre: "Date", rendu: (f) => f.datePiece.slice(0, 10) },
        { cle: "tiers", titre: "Client", rendu: (f) => f.tiersNom ?? "—" },
        { cle: "ttc", titre: "Total TTC", rendu: (f) => f.montantTTC.toLocaleString("fr-FR") },
        { cle: "etat", titre: "Paiement", rendu: (f) => { const b = badgeEtatPaiement(f.etatPaiement); return <Badge label={b.label} variant={b.variant} />; } },
        { cle: "statut", titre: "Statut", rendu: (f) => f.statut },
      ]}
    />
  );
}
```

- [ ] **Step 2 : Écrire la page serveur**

```tsx
// src/app/ventes/factures/page.tsx
import { Shell } from "@/components/Shell";
import { getDossierIdCookie } from "@/lib/dossier-context";
import { listerFactures } from "@/server/factures";
import { FacturesClient } from "./FacturesClient";

export default async function Page() {
  const dossierId = await getDossierIdCookie();
  const factures = dossierId ? await listerFactures(dossierId) : [];
  return (
    <Shell
      module="ventes"
      breadcrumb={[{ label: "Ventes" }, { label: "Factures clients" }]}
      action={<a className="btn primary" href="/ventes/factures/nouvelle">+ Nouvelle facture</a>}
    >
      <FacturesClient factures={factures} />
    </Shell>
  );
}
```

- [ ] **Step 3 : Vérifier build + démarrage**

Run: `npx tsc --noEmit && npm run build` → compile.
Manuel (optionnel) : `npx next dev -p 3000`, ouvrir `/ventes/factures`, vérifier la liste (après seed) et le shell.

- [ ] **Step 4 : Commit**

```bash
git add src/app/ventes/factures/page.tsx src/app/ventes/factures/FacturesClient.tsx
git commit -m "feat(ventes): page liste des factures clients"
```

---

### Task B6 : Primitive `<LineEditor>` + page « Nouvelle facture »

**Files:**
- Create: `src/components/LineEditor.tsx` (Client)
- Create: `src/app/ventes/factures/nouvelle/page.tsx` (Server : charge tiers + taxes + comptes produits)
- Create: `src/app/ventes/factures/nouvelle/NouvelleFactureClient.tsx` (Client)
- Test: `src/components/LineEditor.test.tsx`

**Interfaces:**
- Consumes: `listerTiers`, `listerTaxes`, `listerComptes`, `creerFacture`, `apercuTotaux`.
- Produces (LineEditor) :
```ts
export interface LigneHT { compteNumero: string; libelleLigne: string; montantHT: number; taxeCode: string; }
export function LineEditor(props: {
  comptes: { numero: string; intitule: string }[];
  taxes: { code: string; nom: string; taux: number }[];
  lignes: LigneHT[]; onChange: (l: LigneHT[]) => void;
}): JSX.Element;
```

- [ ] **Step 1 : Écrire le test qui échoue (LineEditor : ajout de ligne)**

```tsx
// src/components/LineEditor.test.tsx
// @vitest-environment jsdom
import { describe, it, expect, vi } from "vitest";
import { render, screen, fireEvent } from "@testing-library/react";
import { LineEditor } from "./LineEditor";

describe("LineEditor", () => {
  it("ajoute une ligne via le bouton", () => {
    const onChange = vi.fn();
    render(<LineEditor comptes={[{ numero: "707000", intitule: "Ventes" }]}
                       taxes={[{ code: "TVA18", nom: "TVA 18%", taux: 18 }]}
                       lignes={[]} onChange={onChange} />);
    fireEvent.click(screen.getByText(/ajouter une ligne/i));
    expect(onChange).toHaveBeenCalled();
    expect(onChange.mock.calls[0][0]).toHaveLength(1);
  });
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npx vitest run src/components/LineEditor.test.tsx`
Expected: FAIL — module introuvable.

- [ ] **Step 3 : Implémenter `<LineEditor>`**

```tsx
// src/components/LineEditor.tsx
"use client";
export interface LigneHT { compteNumero: string; libelleLigne: string; montantHT: number; taxeCode: string; }

export function LineEditor({ comptes, taxes, lignes, onChange }: {
  comptes: { numero: string; intitule: string }[];
  taxes: { code: string; nom: string; taux: number }[];
  lignes: LigneHT[]; onChange: (l: LigneHT[]) => void;
}) {
  const maj = (i: number, patch: Partial<LigneHT>) => onChange(lignes.map((l, j) => (j === i ? { ...l, ...patch } : l)));
  const ajouter = () => onChange([...lignes, { compteNumero: comptes[0]?.numero ?? "", libelleLigne: "", montantHT: 0, taxeCode: taxes[0]?.code ?? "" }]);
  const retirer = (i: number) => onChange(lignes.filter((_, j) => j !== i));
  return (
    <div style={{ overflowX: "auto" }}>
      <table style={{ width: "100%", borderCollapse: "collapse" }}>
        <thead><tr>
          <th style={{ textAlign: "left", padding: 6 }}>Compte</th>
          <th style={{ textAlign: "left", padding: 6 }}>Libellé</th>
          <th style={{ textAlign: "right", padding: 6 }}>Montant HT</th>
          <th style={{ textAlign: "left", padding: 6 }}>Taxe</th>
          <th></th>
        </tr></thead>
        <tbody>
          {lignes.map((l, i) => (
            <tr key={i}>
              <td style={{ padding: 6 }}>
                <select className="input" value={l.compteNumero} onChange={(e) => maj(i, { compteNumero: e.target.value })}>
                  {comptes.map((c) => <option key={c.numero} value={c.numero}>{c.numero} — {c.intitule}</option>)}
                </select>
              </td>
              <td style={{ padding: 6 }}><input className="input" value={l.libelleLigne} onChange={(e) => maj(i, { libelleLigne: e.target.value })} /></td>
              <td style={{ padding: 6, textAlign: "right" }}><input className="input mono" type="number" value={l.montantHT} onChange={(e) => maj(i, { montantHT: Number(e.target.value) })} /></td>
              <td style={{ padding: 6 }}>
                <select className="input" value={l.taxeCode} onChange={(e) => maj(i, { taxeCode: e.target.value })}>
                  <option value="">— sans taxe —</option>
                  {taxes.map((t) => <option key={t.code} value={t.code}>{t.nom}</option>)}
                </select>
              </td>
              <td style={{ padding: 6 }}><button className="btn" onClick={() => retirer(i)} aria-label="Retirer">✕</button></td>
            </tr>
          ))}
        </tbody>
      </table>
      <button className="btn" onClick={ajouter} style={{ marginTop: 8 }}>+ Ajouter une ligne</button>
    </div>
  );
}
```

- [ ] **Step 4 : Vérifier le succès (LineEditor)**

Run: `npx vitest run src/components/LineEditor.test.tsx` → PASS

- [ ] **Step 5 : Page serveur « Nouvelle facture »**

```tsx
// src/app/ventes/factures/nouvelle/page.tsx
import { Shell } from "@/components/Shell";
import { getDossierIdCookie } from "@/lib/dossier-context";
import { prisma } from "@/lib/db";
import { listerTiers } from "@/server/tiers";
import { listerTaxes } from "@/server/taxes";
import { NouvelleFactureClient } from "./NouvelleFactureClient";

export default async function Page() {
  const dossierId = await getDossierIdCookie();
  if (!dossierId) return <Shell module="ventes" breadcrumb={[{ label: "Ventes" }]}><div /></Shell>;
  const [tiers, taxes, comptesProduits, journalVente] = await Promise.all([
    listerTiers(dossierId, { type: "CLIENT" }),
    listerTaxes(dossierId, { usage: "sale" }),
    prisma.compte.findMany({ where: { dossierId, classeNum: 7, statut: "ACTIF" }, select: { numero: true, intitule: true }, orderBy: { numero: "asc" } }),
    prisma.journal.findFirst({ where: { dossierId, type: "sale" }, select: { id: true } }),
  ]);
  return (
    <Shell module="ventes" breadcrumb={[{ label: "Ventes", href: "/ventes/factures" }, { label: "Factures clients", href: "/ventes/factures" }, { label: "Nouvelle" }]}>
      <NouvelleFactureClient
        dossierId={dossierId}
        journalVenteId={journalVente?.id ?? null}
        tiers={tiers.map((t) => ({ id: t.id, nom: t.nom }))}
        taxes={taxes.map((t) => ({ code: t.code, nom: t.nom, taux: Number(t.taux) }))}
        comptes={comptesProduits}
        compteClient="411100"
      />
    </Shell>
  );
}
```

- [ ] **Step 6 : Client « Nouvelle facture »**

```tsx
// src/app/ventes/factures/nouvelle/NouvelleFactureClient.tsx
"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { LineEditor, type LigneHT } from "@/components/LineEditor";
import { apercuTotaux } from "@/lib/ui/facture-ui";
import { creerFacture } from "@/server/taxes";

export function NouvelleFactureClient({ dossierId, journalVenteId, tiers, taxes, comptes, compteClient }: {
  dossierId: string; journalVenteId: string | null;
  tiers: { id: string; nom: string }[];
  taxes: { code: string; nom: string; taux: number }[];
  comptes: { numero: string; intitule: string }[];
  compteClient: string;
}) {
  const router = useRouter();
  const [tiersId, setTiersId] = useState(tiers[0]?.id ?? "");
  const [numero, setNumero] = useState("");
  const [lignes, setLignes] = useState<LigneHT[]>([]);
  const [erreur, setErreur] = useState<string | null>(null);
  const tauxParCode = new Map(taxes.map((t) => [t.code, t.taux]));
  const apercu = apercuTotaux(lignes.map((l) => ({ montantHT: l.montantHT, taux: l.taxeCode ? (tauxParCode.get(l.taxeCode) ?? 0) : 0 })), "XOF");

  async function enregistrer() {
    setErreur(null);
    if (!journalVenteId) { setErreur("Aucun journal de vente configuré."); return; }
    try {
      const f = await creerFacture({
        dossierId, journalId: journalVenteId, numeroPiece: numero || `BR-${Date.now()}`,
        sens: "VENTE", tiersId, compteTiersNumero: compteClient,
        lignes: lignes.map((l) => ({ compteNumero: l.compteNumero, libelleLigne: l.libelleLigne, montantHT: l.montantHT, taxeCode: l.taxeCode || undefined })),
      });
      router.push(`/ventes/factures/${f.id}`);
    } catch (e) {
      setErreur(e instanceof Error ? e.message : "Erreur");
    }
  }

  return (
    <div className="panel" style={{ padding: 16 }}>
      {erreur && <div className="badge warn" style={{ display: "block", marginBottom: 12 }}>{erreur}</div>}
      <div className="field" style={{ display: "flex", gap: 16, marginBottom: 12 }}>
        <label>Client&nbsp;
          <select className="input" value={tiersId} onChange={(e) => setTiersId(e.target.value)}>
            {tiers.map((t) => <option key={t.id} value={t.id}>{t.nom}</option>)}
          </select>
        </label>
        <label>Réf. brouillon&nbsp;<input className="input" value={numero} onChange={(e) => setNumero(e.target.value)} placeholder="(auto à la validation)" /></label>
      </div>
      <LineEditor comptes={comptes} taxes={taxes} lignes={lignes} onChange={setLignes} />
      <div style={{ marginTop: 12, textAlign: "right" }} className="mono">
        HT {apercu.ht.toLocaleString("fr-FR")} · TVA {apercu.tva.toLocaleString("fr-FR")} · <strong>TTC {apercu.ttc.toLocaleString("fr-FR")}</strong>
      </div>
      <div style={{ marginTop: 12 }}>
        <button className="btn primary" onClick={enregistrer} disabled={!tiersId || lignes.length === 0}>Créer la facture (brouillon)</button>
      </div>
    </div>
  );
}
```

- [ ] **Step 7 : Vérifier build + suite**

Run: `npx tsc --noEmit && npx vitest run` → vert
Run: `npm run build` → compile.

- [ ] **Step 8 : Commit**

```bash
git add src/components/LineEditor.tsx src/components/LineEditor.test.tsx src/app/ventes/factures/nouvelle
git commit -m "feat(ventes): saisie nouvelle facture (LineEditor + aperçu TTC)"
```

---

### Task B7 : Page document facture (`/ventes/factures/[id]`)

**Files:**
- Create: `src/app/ventes/factures/[id]/page.tsx` (Server)
- Create: `src/app/ventes/factures/[id]/DocumentClient.tsx` (Client : statusbar, smart buttons, actions)
- Create: `src/server/facture-actions.ts` (server actions d'action : valider/extourner/annuler depuis l'UI)

**Interfaces:**
- Consumes: `getFacture` (A3), `validerPiece`, `extournerPiece`, `annulerPiece`, `<StatusBar>`, `<SmartButton>`, `<Badge>`.
- Produces:
```ts
// src/server/facture-actions.ts
export function actionValider(id: string): Promise<void>;
export function actionExtourner(id: string): Promise<void>;
export function actionAnnuler(id: string): Promise<void>;
```

- [ ] **Step 1 : Server actions d'action (wrappers)**

```ts
// src/server/facture-actions.ts
"use server";
import { validerPiece, extournerPiece, annulerPiece } from "./pieces";
export async function actionValider(id: string): Promise<void> { await validerPiece(id); }
export async function actionExtourner(id: string): Promise<void> { await extournerPiece(id); }
export async function actionAnnuler(id: string): Promise<void> { await annulerPiece(id); }
```

- [ ] **Step 2 : Client document**

```tsx
// src/app/ventes/factures/[id]/DocumentClient.tsx
"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { StatusBar } from "@/components/StatusBar";
import { SmartButton } from "@/components/SmartButton";
import { Badge } from "@/components/Badge";
import { badgeEtatPaiement } from "@/lib/ui/facture-ui";
import { actionValider, actionExtourner, actionAnnuler } from "@/server/facture-actions";
import type { FactureDetail } from "@/server/factures";

export function DocumentClient({ f }: { f: FactureDetail }) {
  const router = useRouter();
  const [erreur, setErreur] = useState<string | null>(null);
  const run = (fn: (id: string) => Promise<void>) => async () => {
    setErreur(null);
    try { await fn(f.id); router.refresh(); }
    catch (e) { setErreur(e instanceof Error ? e.message : "Erreur"); }
  };
  const badge = badgeEtatPaiement(f.etatPaiement);
  const actions = (
    <>
      {f.statut === "BROUILLON" && <button className="btn primary" onClick={run(actionValider)}>Valider</button>}
      {f.statut === "VALIDEE" && f.etatPaiement !== "PAYE" && <a className="btn primary" href={`/ventes/paiements/nouveau?facture=${f.id}`}>Enregistrer un paiement</a>}
      {f.statut === "VALIDEE" && <button className="btn" onClick={run(actionExtourner)}>Extourner</button>}
      {f.statut === "BROUILLON" && <button className="btn" onClick={run(actionAnnuler)}>Annuler</button>}
    </>
  );
  return (
    <div className="panel" style={{ padding: 16 }}>
      {erreur && <div className="badge warn" style={{ display: "block", marginBottom: 12 }}>{erreur}</div>}
      <StatusBar etats={["BROUILLON", "VALIDEE", "ANNULEE"]} courant={f.statut} actions={actions} />
      <div style={{ display: "flex", gap: 8, margin: "12px 0" }}>
        <SmartButton icone="💰" compteur={f.nbPaiements} label="paiements" href={`/ventes/paiements?tiers=${f.tiersId ?? ""}`} />
        <SmartButton icone="🔗" label={f.estLettree ? "Lettré" : "Non lettré"} />
        <Badge label={badge.label} variant={badge.variant} />
      </div>
      <h2 style={{ margin: "8px 0" }}>Facture {f.numeroPiece}</h2>
      <div className="muted" style={{ marginBottom: 12 }}>Client : {f.tiersNom ?? "—"} · Date : {f.datePiece.slice(0, 10)} · Journal : {f.journalCode}</div>
      <div style={{ overflowX: "auto" }}>
        <table style={{ width: "100%", borderCollapse: "collapse" }}>
          <thead><tr>
            <th style={{ textAlign: "left", padding: 6 }}>Compte</th><th style={{ textAlign: "left", padding: 6 }}>Libellé</th>
            <th style={{ textAlign: "right", padding: 6 }}>Débit</th><th style={{ textAlign: "right", padding: 6 }}>Crédit</th>
          </tr></thead>
          <tbody>
            {f.lignes.map((l) => (
              <tr key={l.id}>
                <td style={{ padding: 6 }}>{l.compteNumero}</td><td style={{ padding: 6 }}>{l.libelleLigne}</td>
                <td style={{ padding: 6, textAlign: "right" }} className="mono">{l.debit ? l.debit.toLocaleString("fr-FR") : "—"}</td>
                <td style={{ padding: 6, textAlign: "right" }} className="mono">{l.credit ? l.credit.toLocaleString("fr-FR") : "—"}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
      <div style={{ marginTop: 12, textAlign: "right" }} className="mono">
        HT {f.montantHT.toLocaleString("fr-FR")} · TVA {f.montantTVA.toLocaleString("fr-FR")} · <strong>TTC {f.montantTTC.toLocaleString("fr-FR")}</strong>
      </div>
    </div>
  );
}
```

- [ ] **Step 3 : Page serveur document**

```tsx
// src/app/ventes/factures/[id]/page.tsx
import { Shell } from "@/components/Shell";
import { getDossierIdCookie } from "@/lib/dossier-context";
import { getFacture } from "@/server/factures";
import { DocumentClient } from "./DocumentClient";

export default async function Page({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const dossierId = await getDossierIdCookie();
  if (!dossierId) return <Shell module="ventes" breadcrumb={[{ label: "Ventes" }]}><div /></Shell>;
  const f = await getFacture(dossierId, id);
  return (
    <Shell module="ventes" breadcrumb={[{ label: "Ventes", href: "/ventes/factures" }, { label: "Factures clients", href: "/ventes/factures" }, { label: f.numeroPiece }]}>
      <DocumentClient f={f} />
    </Shell>
  );
}
```

- [ ] **Step 4 : Vérifier build + suite**

Run: `npx tsc --noEmit && npx vitest run && npm run build` → vert.
Manuel (optionnel) : créer une facture, la valider, vérifier statusbar + smart buttons + lignes générées.

- [ ] **Step 5 : Commit**

```bash
git add src/app/ventes/factures/[id] src/server/facture-actions.ts
git commit -m "feat(ventes): document facture (statusbar, smart buttons, actions)"
```

---

### Task B8 : Drawer paiement (`/ventes/paiements/nouveau`)

**Files:**
- Create: `src/app/ventes/paiements/nouveau/page.tsx` (Server : charge la facture cible + journaux trésorerie + comptes trésorerie)
- Create: `src/app/ventes/paiements/nouveau/PaiementClient.tsx` (Client)
- Create: `src/app/ventes/paiements/page.tsx` (liste paiements, Server + DataTable)
- Modify: `src/server/factures.ts` (ajout `listerPaiements` si absent — voir interface)

**Interfaces:**
- Consumes: `getFacture`, `enregistrerPaiement`, `prisma.journal` (type cash/bank), `prisma.compte` (classe 5).
- Produces:
```ts
// src/server/factures.ts (append)
export interface PaiementListItem { id: string; numeroPiece: string; date: string; tiersNom: string | null; montant: number; sens: string; }
export function listerPaiements(dossierId: string, filtre?: { tiersId?: string }): Promise<PaiementListItem[]>;
```

- [ ] **Step 1 : Test serveur `listerPaiements` (échoue)**

```ts
// src/server/factures.test.ts (append)
import { listerPaiements } from "./factures";
import { enregistrerPaiement as encaisser } from "./paiements";

it("listerPaiements renvoie les paiements du dossier", async () => {
  const client = await creerTiers({ dossierId, code: "C001", nom: "Alpha", type: "CLIENT" });
  await prisma.journal.create({ data: { code: "CAI", libelle: "Caisse", type: "cash", dossierId } });
  const f = await creerFacture({ dossierId, journalId: journalVente, numeroPiece: "VT-7", sens: "VENTE",
    tiersId: client.id, compteTiersNumero: "411100",
    lignes: [{ compteNumero: "707000", libelleLigne: "V", montantHT: 100_000, taxeCode: "TVA18" }] });
  await validerPiece(f.id);
  const jc = (await prisma.journal.findFirstOrThrow({ where: { dossierId, code: "CAI" } })).id;
  await encaisser({ dossierId, journalId: jc, numeroPiece: "CAI-7", sens: "ENTRANT",
    tiersId: client.id, compteTresorerieNumero: "521000", compteTiersNumero: "411100", montant: 50_000 });
  const ps = await listerPaiements(dossierId);
  expect(ps).toHaveLength(1);
  expect(ps[0].montant).toBe(50_000);
  expect(ps[0].tiersNom).toBe("Alpha");
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npx vitest run src/server/factures.test.ts -t listerPaiements`
Expected: FAIL — `listerPaiements` non exporté.

- [ ] **Step 3 : Implémenter `listerPaiements`**

```ts
// src/server/factures.ts (append)
export interface PaiementListItem { id: string; numeroPiece: string; date: string; tiersNom: string | null; montant: number; sens: string; }

export async function listerPaiements(dossierId: string, filtre: { tiersId?: string } = {}): Promise<PaiementListItem[]> {
  const ps = await prisma.paiement.findMany({
    where: { dossierId, ...(filtre.tiersId ? { tiersId: filtre.tiersId } : {}) },
    include: { tiers: { select: { nom: true } }, piece: { select: { numeroPiece: true } } },
    orderBy: { createdAt: "desc" },
  });
  return ps.map((p) => ({
    id: p.id, numeroPiece: p.piece?.numeroPiece ?? "—", date: p.datePaiement.toISOString(),
    tiersNom: p.tiers?.nom ?? null, montant: Number(p.montant), sens: p.sens,
  }));
}
```

- [ ] **Step 4 : Vérifier le succès**

Run: `npx vitest run src/server/factures.test.ts -t listerPaiements` → PASS

- [ ] **Step 5 : Client paiement (drawer-form)**

```tsx
// src/app/ventes/paiements/nouveau/PaiementClient.tsx
"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { enregistrerPaiement } from "@/server/paiements";

export function PaiementClient({ dossierId, factureId, tiersId, compteClient, residuel, journaux, comptesTresorerie }: {
  dossierId: string; factureId: string; tiersId: string; compteClient: string; residuel: number;
  journaux: { id: string; libelle: string }[]; comptesTresorerie: { numero: string; intitule: string }[];
}) {
  const router = useRouter();
  const [montant, setMontant] = useState(residuel);
  const [journalId, setJournalId] = useState(journaux[0]?.id ?? "");
  const [compte, setCompte] = useState(comptesTresorerie[0]?.numero ?? "");
  const [erreur, setErreur] = useState<string | null>(null);

  async function valider() {
    setErreur(null);
    try {
      await enregistrerPaiement({
        dossierId, journalId, numeroPiece: `PAY-${Date.now()}`, sens: "ENTRANT",
        tiersId, compteTresorerieNumero: compte, compteTiersNumero: compteClient, montant,
      });
      router.push(`/ventes/factures/${factureId}`);
    } catch (e) { setErreur(e instanceof Error ? e.message : "Erreur"); }
  }

  return (
    <div className="panel" style={{ padding: 16, maxWidth: 480 }}>
      {erreur && <div className="badge warn" style={{ display: "block", marginBottom: 12 }}>{erreur}</div>}
      <h3>Encaisser la facture</h3>
      <label className="field">Montant&nbsp;<input className="input mono" type="number" value={montant} onChange={(e) => setMontant(Number(e.target.value))} /></label>
      <label className="field">Journal&nbsp;
        <select className="input" value={journalId} onChange={(e) => setJournalId(e.target.value)}>
          {journaux.map((j) => <option key={j.id} value={j.id}>{j.libelle}</option>)}
        </select>
      </label>
      <label className="field">Compte trésorerie&nbsp;
        <select className="input" value={compte} onChange={(e) => setCompte(e.target.value)}>
          {comptesTresorerie.map((c) => <option key={c.numero} value={c.numero}>{c.numero} — {c.intitule}</option>)}
        </select>
      </label>
      <div style={{ marginTop: 12 }}><button className="btn primary" onClick={valider} disabled={!journalId || !compte || montant <= 0}>Encaisser</button></div>
    </div>
  );
}
```

- [ ] **Step 6 : Page serveur paiement + liste paiements**

```tsx
// src/app/ventes/paiements/nouveau/page.tsx
import { Shell } from "@/components/Shell";
import { getDossierIdCookie } from "@/lib/dossier-context";
import { prisma } from "@/lib/db";
import { getFacture } from "@/server/factures";
import { PaiementClient } from "./PaiementClient";

export default async function Page({ searchParams }: { searchParams: Promise<{ facture?: string }> }) {
  const { facture } = await searchParams;
  const dossierId = await getDossierIdCookie();
  if (!dossierId || !facture) return <Shell module="ventes" breadcrumb={[{ label: "Ventes" }]}><div>Facture manquante.</div></Shell>;
  const f = await getFacture(dossierId, facture);
  const ligneClient = f.lignes.find((l) => l.compteNumero.startsWith("411"));
  const [journaux, comptesTresorerie] = await Promise.all([
    prisma.journal.findMany({ where: { dossierId, type: { in: ["cash", "bank"] } }, select: { id: true, libelle: true } }),
    prisma.compte.findMany({ where: { dossierId, classeNum: 5, statut: "ACTIF" }, select: { numero: true, intitule: true } }),
  ]);
  const residuel = f.montantTTC; // simplifié : résiduel ≈ TTC si non payé (le backend lettre au réel)
  return (
    <Shell module="ventes" breadcrumb={[{ label: "Ventes", href: "/ventes/factures" }, { label: f.numeroPiece, href: `/ventes/factures/${f.id}` }, { label: "Paiement" }]}>
      <PaiementClient dossierId={dossierId} factureId={f.id} tiersId={f.tiersId ?? ""} compteClient={ligneClient?.compteNumero ?? "411100"} residuel={residuel} journaux={journaux} comptesTresorerie={comptesTresorerie} />
    </Shell>
  );
}
```

```tsx
// src/app/ventes/paiements/page.tsx
import { Shell } from "@/components/Shell";
import { getDossierIdCookie } from "@/lib/dossier-context";
import { listerPaiements } from "@/server/factures";

export default async function Page({ searchParams }: { searchParams: Promise<{ tiers?: string }> }) {
  const { tiers } = await searchParams;
  const dossierId = await getDossierIdCookie();
  const paiements = dossierId ? await listerPaiements(dossierId, { tiersId: tiers || undefined }) : [];
  return (
    <Shell module="ventes" breadcrumb={[{ label: "Ventes", href: "/ventes/factures" }, { label: "Paiements" }]}>
      <div style={{ overflowX: "auto" }}>
        <table style={{ width: "100%", borderCollapse: "collapse" }}>
          <thead><tr><th style={{ textAlign: "left", padding: 8 }}>Pièce</th><th style={{ textAlign: "left", padding: 8 }}>Date</th><th style={{ textAlign: "left", padding: 8 }}>Tiers</th><th style={{ textAlign: "right", padding: 8 }}>Montant</th></tr></thead>
          <tbody>
            {paiements.length === 0 ? <tr><td colSpan={4} className="muted" style={{ padding: 16 }}>Aucun paiement.</td></tr> :
              paiements.map((p) => <tr key={p.id}><td style={{ padding: 8 }}>{p.numeroPiece}</td><td style={{ padding: 8 }}>{p.date.slice(0, 10)}</td><td style={{ padding: 8 }}>{p.tiersNom ?? "—"}</td><td style={{ padding: 8, textAlign: "right" }} className="mono">{p.montant.toLocaleString("fr-FR")}</td></tr>)}
          </tbody>
        </table>
      </div>
    </Shell>
  );
}
```

- [ ] **Step 7 : Vérifier build + suite**

Run: `npx tsc --noEmit && npx vitest run && npm run build` → vert.

- [ ] **Step 8 : Commit**

```bash
git add src/server/factures.ts src/server/factures.test.ts src/app/ventes/paiements
git commit -m "feat(ventes): encaissement (drawer paiement + liste paiements)"
```

---

### Task B9 : Page balance âgée + grand-livre auxiliaire (`/ventes/balance-agee`)

**Files:**
- Create: `src/app/ventes/balance-agee/page.tsx` (Server)

**Interfaces:**
- Consumes: `getBalanceAgee` (existant), `getDossierIdCookie`, `<Shell>`.

- [ ] **Step 1 : Page serveur**

```tsx
// src/app/ventes/balance-agee/page.tsx
import { Shell } from "@/components/Shell";
import { getDossierIdCookie } from "@/lib/dossier-context";
import { getBalanceAgee } from "@/server/auxiliaire";

export default async function Page() {
  const dossierId = await getDossierIdCookie();
  const lignes = dossierId ? await getBalanceAgee(dossierId) : [];
  return (
    <Shell module="ventes" breadcrumb={[{ label: "Ventes", href: "/ventes/factures" }, { label: "Balance âgée" }]}>
      <div style={{ overflowX: "auto" }}>
        <table style={{ width: "100%", borderCollapse: "collapse" }}>
          <thead><tr>
            <th style={{ textAlign: "left", padding: 8 }}>Tiers</th>
            <th style={{ textAlign: "right", padding: 8 }}>0–30 j</th><th style={{ textAlign: "right", padding: 8 }}>31–60 j</th>
            <th style={{ textAlign: "right", padding: 8 }}>61–90 j</th><th style={{ textAlign: "right", padding: 8 }}>+90 j</th>
            <th style={{ textAlign: "right", padding: 8 }}>Total</th>
          </tr></thead>
          <tbody>
            {lignes.length === 0 ? <tr><td colSpan={6} className="muted" style={{ padding: 16 }}>Aucun encours.</td></tr> :
              lignes.map((l) => (
                <tr key={l.tiersId}>
                  <td style={{ padding: 8 }}>{l.tiersCode} — {l.tiersNom}</td>
                  <td style={{ padding: 8, textAlign: "right" }} className="mono">{l.b0_30.toLocaleString("fr-FR")}</td>
                  <td style={{ padding: 8, textAlign: "right" }} className="mono">{l.b31_60.toLocaleString("fr-FR")}</td>
                  <td style={{ padding: 8, textAlign: "right" }} className="mono">{l.b61_90.toLocaleString("fr-FR")}</td>
                  <td style={{ padding: 8, textAlign: "right" }} className="mono">{l.b90plus.toLocaleString("fr-FR")}</td>
                  <td style={{ padding: 8, textAlign: "right" }} className="mono"><strong>{l.total.toLocaleString("fr-FR")}</strong></td>
                </tr>
              ))}
          </tbody>
        </table>
      </div>
    </Shell>
  );
}
```

- [ ] **Step 2 : Vérifier build**

Run: `npx tsc --noEmit && npm run build` → compile.

- [ ] **Step 3 : Commit**

```bash
git add src/app/ventes/balance-agee/page.tsx
git commit -m "feat(ventes): page balance âgée clients"
```

---

### Task B10 : Page Tiers (`/tiers`) — support de la verticale

**Files:**
- Create: `src/app/tiers/page.tsx` (Server : liste + formulaire création)
- Create: `src/app/tiers/TiersClient.tsx` (Client)

**Interfaces:**
- Consumes: `listerTiers`, `creerTiers`, `<Shell>`, `<DataTable>`.

- [ ] **Step 1 : Client tiers (liste + création)**

```tsx
// src/app/tiers/TiersClient.tsx
"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { DataTable } from "@/components/DataTable";
import { creerTiers } from "@/server/tiers";

type T = { id: string; code: string; nom: string; type: string };

export function TiersClient({ tiers }: { tiers: T[] }) {
  const router = useRouter();
  const [code, setCode] = useState(""); const [nom, setNom] = useState(""); const [type, setType] = useState("CLIENT");
  const [erreur, setErreur] = useState<string | null>(null);
  async function ajouter() {
    setErreur(null);
    try { await creerTiers({ dossierId: "", code, nom, type: type as "CLIENT" | "FOURNISSEUR" | "AUTRE" }); }
    catch (e) { setErreur(e instanceof Error ? e.message : "Erreur"); return; }
    setCode(""); setNom(""); router.refresh();
  }
  return (
    <div>
      <div className="panel" style={{ padding: 12, marginBottom: 16, display: "flex", gap: 8, alignItems: "end" }}>
        <label className="field">Code&nbsp;<input className="input" value={code} onChange={(e) => setCode(e.target.value)} /></label>
        <label className="field">Nom&nbsp;<input className="input" value={nom} onChange={(e) => setNom(e.target.value)} /></label>
        <label className="field">Type&nbsp;
          <select className="input" value={type} onChange={(e) => setType(e.target.value)}>
            <option value="CLIENT">Client</option><option value="FOURNISSEUR">Fournisseur</option><option value="AUTRE">Autre</option>
          </select>
        </label>
        <button className="btn primary" onClick={ajouter} disabled={!code || !nom}>+ Ajouter</button>
        {erreur && <span className="badge warn">{erreur}</span>}
      </div>
      <DataTable lignes={tiers} cleLigne={(t) => t.id} rechercheTexte videLabel="Aucun tiers."
        colonnes={[
          { cle: "code", titre: "Code", rendu: (t) => t.code },
          { cle: "nom", titre: "Nom", rendu: (t) => t.nom },
          { cle: "type", titre: "Type", rendu: (t) => t.type },
        ]} />
    </div>
  );
}
```

> NOTE : `creerTiers` exige le `dossierId`. Le passer en prop depuis la page (le client ne lit pas le cookie). Corriger l'appel : voir Step 2 (la page passe `dossierId`, le client l'utilise).

- [ ] **Step 2 : Corriger le client pour recevoir `dossierId` en prop**

Remplacer la signature et l'appel dans `TiersClient.tsx` :
```tsx
export function TiersClient({ tiers, dossierId }: { tiers: T[]; dossierId: string }) {
  // ...
  await creerTiers({ dossierId, code, nom, type: type as "CLIENT" | "FOURNISSEUR" | "AUTRE" });
  // ...
}
```

- [ ] **Step 3 : Page serveur tiers**

```tsx
// src/app/tiers/page.tsx
import { Shell } from "@/components/Shell";
import { getDossierIdCookie } from "@/lib/dossier-context";
import { listerTiers } from "@/server/tiers";
import { TiersClient } from "./TiersClient";

export default async function Page() {
  const dossierId = await getDossierIdCookie();
  const tiers = dossierId ? await listerTiers(dossierId) : [];
  return (
    <Shell module="tiers" breadcrumb={[{ label: "Tiers" }]}>
      <TiersClient tiers={tiers} dossierId={dossierId ?? ""} />
    </Shell>
  );
}
```

- [ ] **Step 4 : Vérifier build + suite**

Run: `npx tsc --noEmit && npx vitest run && npm run build` → vert.

- [ ] **Step 5 : Commit**

```bash
git add src/app/tiers
git commit -m "feat(tiers): page liste + création de tiers"
```

---

### Task B11 : Redirection d'accueil + lien États + recette manuelle

**Files:**
- Modify: `src/app/page.tsx` (tableau de bord : liens vers Ventes + sélection dossier ; ou redirection /ventes/factures)
- Create: `src/app/etats/page.tsx` (placeholder T1 : lien vers déclaration TVA via `getDeclarationTVA`)

**Interfaces:**
- Consumes: `getDeclarationTVA`, `<Shell>`.

- [ ] **Step 1 : Page États (déclaration TVA)**

```tsx
// src/app/etats/page.tsx
import { Shell } from "@/components/Shell";
import { getDossierIdCookie } from "@/lib/dossier-context";
import { getDeclarationTVA } from "@/server/taxes";

export default async function Page() {
  const dossierId = await getDossierIdCookie();
  const tva = dossierId ? await getDeclarationTVA(dossierId) : { collectee: 0, deductible: 0, netteDue: 0 };
  return (
    <Shell module="etats" breadcrumb={[{ label: "États" }, { label: "Déclaration TVA" }]}>
      <div className="panel" style={{ padding: 16, maxWidth: 420 }}>
        <h3>Déclaration de TVA</h3>
        <p>TVA collectée : <strong className="mono">{tva.collectee.toLocaleString("fr-FR")}</strong></p>
        <p>TVA déductible : <strong className="mono">{tva.deductible.toLocaleString("fr-FR")}</strong></p>
        <p>TVA nette due : <strong className="mono">{tva.netteDue.toLocaleString("fr-FR")}</strong></p>
      </div>
    </Shell>
  );
}
```

- [ ] **Step 2 : Tableau de bord d'accueil**

Remplacer `src/app/page.tsx` par un tableau de bord minimal sous `<Shell module="ventes">` listant des liens rapides (Factures, Paiements, Balance âgée, Tiers, États) :
```tsx
import { Shell } from "@/components/Shell";
export default function Page() {
  const liens = [
    { href: "/ventes/factures", label: "Factures clients" },
    { href: "/ventes/paiements", label: "Paiements" },
    { href: "/ventes/balance-agee", label: "Balance âgée" },
    { href: "/tiers", label: "Tiers" },
    { href: "/etats", label: "Déclaration TVA" },
  ];
  return (
    <Shell module="ventes" breadcrumb={[{ label: "Tableau de bord" }]}>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill,minmax(200px,1fr))", gap: 12 }}>
        {liens.map((l) => <a key={l.href} href={l.href} className="panel" style={{ padding: 16, textDecoration: "none", color: "inherit" }}>{l.label}</a>)}
      </div>
    </Shell>
  );
}
```

- [ ] **Step 3 : Recette manuelle de bout en bout (DoD)**

Run: `npx next dev -p 3000`. Parcours :
1. Sélectionner le dossier « Les Associés SA ».
2. Tiers → ajouter/voir un client.
3. Ventes → + Nouvelle facture → client + ligne 707 HT 100 000 + TVA18 → Créer.
4. Document → Valider (numéro VT/2020/0001, statusbar → Validé).
5. Enregistrer un paiement → 118 000 → retour document : état « Payé », smart button 💰 1, 🔗 Lettré.
6. Balance âgée : le tiers a 0 (soldé). États : TVA collectée 18 000.

- [ ] **Step 4 : Vérifier build + suite finale**

Run: `npx vitest run && npx tsc --noEmit && npm run build` → tout vert.

- [ ] **Step 5 : Commit**

```bash
git add src/app/page.tsx src/app/etats/page.tsx
git commit -m "feat(ui): tableau de bord d'accueil + page déclaration TVA"
```

---

## Self-Review (couverture du spec)

- §3.1 Routing → Tasks B5–B11 (ventes/factures, [id], paiements, balance-agee, tiers, etats). ✓
- §3.2 Primitives → B2 (Shell), B3 (DataTable), B4 (StatusBar/SmartButton/Badge), B6 (LineEditor). ✓
- §3.3 Shell (switcher module/dossier, breadcrumb, sous-menu, aucun-dossier) → B2. ✓
- §4.1 Liste factures → B5. §4.2 Document (statusbar, smart buttons, lignes) → B7. §4.3 Flux paiement → B8. §4.4 Balance âgée + GL auxiliaire → B9 (+ États TVA B11). ✓
- §5 Ajouts backend (Journal.type, listerFactures, getFacture, listerPaiements) → A1, A2, A3, B8. ✓
- §6 États/erreurs → bannières dans clients (B6/B7/B8/B10), boutons désactivés (B7), aucun-dossier (B2). ✓
- §7 Tests : server (A1/A2/A3/B8), mappers purs (B1), composants jsdom (B0/B3/B4/B6). ✓
- §8 DoD → recette B11 Step 3. ✓

Note de cohérence : `creerFacture` accepte `taxeCode?: string` (optionnel) — le client envoie `l.taxeCode || undefined`. `getEtatPaiementFacture` importé depuis `./paiements`. `EtatPaiementFacture` réexporté implicitement via le type de retour.
